# Installation

To use Solid X in your project, run one of the following commands in your terminal:

<TabsSnippets>
  <TabsSnippets.List>
    <TabsSnippets.Trigger value="npm">npm</TabsSnippets.Trigger>
    <TabsSnippets.Trigger value="yarn">yarn</TabsSnippets.Trigger>
    <TabsSnippets.Trigger value="pnpm">pnpm</TabsSnippets.Trigger>
  </TabsSnippets.List>
  <TabsSnippets.Content value="npm">
    {/* <!-- prettier-ignore-start --> */}
    ```sh
    npm install @solid-x/material
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
  <TabsSnippets.Content value="yarn">
    {/* <!-- prettier-ignore-start --> */}
    ```sh
    yarn add @solid-x/material
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
  <TabsSnippets.Content value="pnpm">
    {/* <!-- prettier-ignore-start --> */}
    ```sh
    pnpm add @solid-x/material
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
</TabsSnippets>

## Peer Dependencies

Please note that [solid-js](https://solidjs.com) is a peer dependency:

```json
"peerDependencies": {
  "solid-js": "^1.7.0"
}
```

## Typography

Material You is designed to use [Roboto](https://fonts.google.com/specimen/Roboto) by default. You may
add it to your project using [Fontsource](https://fontsource.org/fonts/roboto), or using the
Google Web Fonts CDN.

### Fontsource

You may add it to your project via [Fontsource](https://fontsource.org/fonts/roboto) by running
one of the commads below:

<TabsSnippets>
  <TabsSnippets.List>
    <TabsSnippets.Trigger value="npm">npm</TabsSnippets.Trigger>
    <TabsSnippets.Trigger value="yarn">yarn</TabsSnippets.Trigger>
    <TabsSnippets.Trigger value="pnpm">pnpm</TabsSnippets.Trigger>
  </TabsSnippets.List>
  <TabsSnippets.Content value="npm">
    {/* <!-- prettier-ignore-start --> */}
    ```sh
    npm install @fontsource/roboto
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
  <TabsSnippets.Content value="yarn">
    {/* <!-- prettier-ignore-start --> */}
    ```sh
    yarn install @fontsource/roboto
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
  <TabsSnippets.Content value="pnpm">
    {/* <!-- prettier-ignore-start --> */}
    ```sh
    pnpm install @fontsource/roboto
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
</TabsSnippets>

Then you can import it in your entry point like:
```js
import '@fontsource/roboto/300.css';
import '@fontsource/roboto/400.css';
import '@fontsource/roboto/500.css';
import '@fontsource/roboto/700.css';
```
<Callout type="info">
Fontsource can be configured to load specific subsets, weights and styles.
Material UI's default typography configuration relies only on the 300, 400, 500, and 700 font weights.
</Callout>

### Google Web Fonts

To install the Roboto font using the Google Web Fonts CDN, simply add the following
snippet to your project's `<head></head>` tag:

```html
<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" />
```

## Icons

### Google Web Fonts

To install the Material Icons font using the Google Web Fonts CDN, simply add the following
snippet to your project's `<head></head>` tag:

```html
<link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons" />
```

Then you can use the Icons like:

```html
<span class="material-icons-outlined"> account_circle </span>
```
