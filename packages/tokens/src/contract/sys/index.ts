import { default as color } from './color.contract';
import { default as elevation } from './elevation.contract';
import { default as motion } from './motion.contract';
import { default as shape } from './shape.contract';
import { default as typescale } from './typescale.contract';

const sys = {
  color,
  elevation,
  motion,
  shape,
  typescale
};

export default sys;
